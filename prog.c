/* ~/projects/sys_prog/
 * Help stay focused on development.
 * Create repo for C code. Add books to repo.
 *
 * Compiler: x86_64-w64-mingw32-gcc.exe
 * Bash: C:\cygwin64\Cygwin.bat
 *  
 * How is data passed on the stack?
 * Memory management.
 * More on stacks, buffer, stack smashing, etc...
 * Time and Space Complexity
 * Dynamic programming.
 * 
 * K&R, Algorithms, OS Dev
 */

#include <stdio.h>

int main(int argc, char** argv)
{
    int i = 0;
    for(i = 0; i < 10; i++) {
        printf("%d\n", i);
    }
}

int gcd(int u, int v)
{
    int t;
    while(u < v) {

    }
    return v;
}